# CLAUDE.md — TikiTakaFootyTV

Context for any Claude working on this project. Read this first.

## What this is

A **faceless soccer theme page** (Instagram / YouTube / TikTok) launching around the **FIFA 2026 World Cup**, then continuing with general soccer content. Brand: **TikiTakaFootyTV** (backup: TikiTakaFootyHQ). The owner wants an **automated content pipeline** so producing posts takes very little of their time.

**Content identity (as of 2026-06-12): DAILY WORLD CUP NEWS & STORIES.** The channel tells the day's biggest football moments as fast, cinematic narratives — heroes, upsets, redemption arcs, selection drama, talking points (the Pulisic "redemption" video is the model). This is a pivot away from the original **quizzes/stats/trivia** framing; quizzes/stats remain in the toolkit (e.g. the proven `quiz_top5` outlier) but are secondary. The copyright-safe / no-footage rule is unchanged — stories are still told with stat graphics + AI imagery + VO, never broadcast clips.

The repo has a working pipeline (brain skills + deterministic render/publish glue — see the repo map and status below); design docs here capture the strategy behind it.

## The one non-negotiable principle: copyright-safe, no-footage

Soccer is the most aggressively copyright-enforced content online (FIFA/UEFA/PL + Content ID + DMCA). Reposting match footage — **including from FIFA's own YouTube** — gets struck and kills channels. So the entire strategy avoids broadcast footage:

- **Core formats use NO match footage.** Videos are generated **stats graphics + AI imagery + voiceover**. (Stat cards, ranking countdowns, trivia/"did-you-know", season-in-numbers, tactical diagrams.)
- Sanctioned footage paths exist only via **official FIFA 2026 creator programs** — TikTok (Creator Correspondents) and YouTube (now a "Preferred Platform" + a selected creator cohort). Both are **selective/invite-based**, not open to a new page; licensed footage stays with media partners + the chosen cohort. Aspire to / apply, but don't design around having it. (YouTube being a co-equal WC platform raises **YouTube Shorts as a launch priority**.)
- Short fair-use clips (≤~10s, own VO, muted audio) are an *optional, calculated-risk* add-on — **never the foundation**. Content ID can still flag even valid fair use.
- Avoid generating recognizable player **faces** (right-of-publicity + AI mangles them). Lead with stat cards, kits, crests, silhouettes.

If a future request drifts toward "repost highlights," push back and redirect to the no-footage model.

## The pipeline (the mental model)

Claude is the **brain** (judgment), code is the **deterministic glue**:

```
data/news/stats → CLAUDE (structured VideoSpec JSON) → Nano Banana graphics
   + ElevenLabs VO + Remotion assembly + burned-in captions → finished post
```

Two feeder engines surround the brain: the **grounding/news engine** (`soccer-news` skill + `kb/`) tracks what's *true & current* (dated, sourced facts + live narratives), and the **outlier-discovery engine** (YouTube Data API, spec'd) tracks what's *viral*. The brain generates **grounded × viral** specs. Loop: *mine what's true + what's working → rebuild it copyright-safe*.

## Tool stack

| Stage | Tool | Notes |
|---|---|---|
| Ideation / script / captions | **Claude Code** (subscription) via the `videospec` skill | runs on the subscription, NOT the metered API; `VideoSpec` schema is the validation contract |
| Voiceover | **ElevenLabs** | `elevenlabs/skills@text-to-speech` |
| Captions | **ElevenLabs STT** | `@speech-to-text` |
| Graphics / thumbnails | **Nano Banana** (Gemini image API) | free tier ~500 img/day; `banana-claude` skill; Pro tier paid (~$0.13/img) for best text |
| Video assembly | **Remotion** or **FFmpeg** | animates stills + VO + captions |
| B-roll (optional, phase 2) | **Higgsfield** | AI cinematic atmosphere; pricey |
| Outlier discovery | **YouTube Data API v3** | free, 10k units/day |

**API keys (in `.env`, see `.env.example`; obtained + validated ✓):** `GEMINI_API_KEY` (⚠️ **image API needs billing — free tier = 0 for the image model**, despite blog claims), `ELEVENLABS_API_KEY` (works; ⚠️ **free tier has no commercial rights — monetized page needs ≥ Starter $5/mo**), `YOUTUBE_API_KEY` (works, free). All on one Google account except ElevenLabs. **No Anthropic key** — scripting runs on the Claude Code subscription.

## Repo map

| File | What it is |
|---|---|
| `CLAUDE.md` | This file — project context |
| `PLAYBOOK.md` | How the Claude content engine works: VideoSpec schema, cost engineering, visual sourcing, tool stack, build order |
| `VIRAL-FORMULA.md` | Reverse-engineered viral formula from real faceless soccer pages + 5 starter templates |
| `OUTLIER-ENGINE-SPEC.md` | Spec for the outlier-discovery engine (replicates Subscribr's hardest feature) |
| `seeds.json` | Seed list of ~22 soccer YouTube channels (by lane/priority) for the outlier engine |
| `videospec_schema.py` | The `VideoSpec` Pydantic contract the downstream pipeline validates against (no API call) |
| `.claude/skills/videospec/` | **The brain** — generates a VideoSpec from a topic (grounds first), on the subscription |
| `kb/` | **Canonical knowledge base** — dated, sourced facts + narratives for grounding (schema: `kb/schema.md`, scope: `kb/watchlist.json`) |
| `.claude/skills/soccer-news/` | **Grounding engine** — refreshes the KB + grounds a topic before scripting |
| `.claude/skills/fact-check/` | **Fact-check stage** — verifies a spec's claims (esp. superlatives) vs the KB before production |
| `.claude/skills/pick-images/` | **Vision image selection** — Claude looks at candidates + picks the correct one per `commons` scene |
| `.claude/skills/analytics-review/` | **The learning loop** — reviews each published video's analytics (retention/swipe-away/conversion), distills durable LEARNINGS into `kb/learnings.json` that `videospec` auto-applies. `review` mode (interpret + update) / `brief` mode (what videospec reads). On-demand; the deterministic data pull is `youtube_analytics.py collect` |
| `analytics/performance.jsonl` | **Performance store** — one record per published video joining its spec's design levers (format/hook_type/retention_mechanic/scenes) with its YouTube analytics (retention curve, swipe-away, conversion) + scene-leak attribution. Written by `youtube_analytics.py collect`, read by `analytics-review` |
| `kb/learnings.json` | **Distilled learnings** — dated, evidenced findings (`n`/`confidence`/`proposed_rule_change`) + `channel_baseline`, maintained by `analytics-review`, read by `videospec` (auto-applies `high`-confidence, weights `low`/`med`). Supersedes the old hand-typed "video #1" calibration block |
| `pipeline/generate_assets.py` | ElevenLabs VO (+ optional Nano Banana) asset generation, via `uv` |
| `pipeline/remotion/` | Remotion project — composes graphics + VO + captions → MP4 |
| `pipeline/outlier_ingest.py` | **Viral engine MVP** — YouTube outlier discovery (deterministic), ranks `seeds.json` channels |
| `pipeline/fetch_images.py` | CC/Wikimedia **free** image fetcher (players/stadiums/nations) → KB `image` field |
| `pipeline/cutout.py` | Background removal (`rembg`) → transparent-PNG **cutout** for the iPhone-sticker corner element (`out/cutouts/<slug>.png`) |
| `pipeline/ensure_assets.py` | **Auto-fetch + auto-cutout** — creates stub entities, fetches CC images, cuts out the subject for any spec (hands-off) |
| `pipeline/fetch_candidates.py` | Fetch several Commons options for one query → vision-vet → curate a KB entity |
| `pipeline/fetch_player_candidates_plus.py` | **Expanded-source player photos** — when Commons/Openverse give only club shots or low-res, this casts wider: Wikidata P18 (preferred image) + per-language Wikipedia LEADS (en/ko/cs/de/es/tr — a Korean/Czech player's ko/cs page lead is often the NATIONAL-kit shot Commons search misses). Free-licensed Commons-hosted files only. Usage: `fetch_player_candidates_plus.py <slug>:"<Full Name>" …` → `out/candidates/<slug>/plus-N`. Found Souček's Czech-kit + Coufal's national-kit shots the base fetcher missed. |
| `pipeline/fetch_player_candidates.py` | **Wider-net player photos** — gathers many free candidates (Commons search + Commons *category* + Openverse/Flickr-CC) so you can vision-pick a NATIONAL-kit shot. Note: national-kit photos are usually crowded match shots → rembg grabs the group; crop to the lone player first, or use a clean club/training portrait |
| `pipeline/fetch_scene_candidates.py` | Per-spec candidate fetch for one-off `commons` scenes (feeds `pick-images`) |
| `pipeline/make_video.sh` | **One-command driver:** `spec.json → finished MP4` (ensure → VO → visuals → render). **Draft is the DEFAULT** (free Piper VO, `-draft` output, no ElevenLabs credits); real ElevenLabs VO is opt-in via `TTV_PRODUCTION=1`. ALWAYS draft + review before a production run. |
| `pipeline/storyboard.sh` | **Pre-render preview driver:** `spec.json → out/storyboards/<stem>.html` (ensure → resolve visuals → HTML). Runs only the FREE/FAST half — **no VO, no Remotion render** — so the owner sees which image/background each scene will get BEFORE the slow draft. Iterate the spec here for free; the images it downloads are cached so the eventual draft renders faster. Driven by `/storyboard`. |
| `pipeline/remotion/storyboard.mjs` | **Storyboard HTML generator** — reads `storyboard-props.json` (from `prepare.mjs` in `TTV_STORYBOARD` mode) + the spec, emits a 9:16-card-per-scene contact sheet: the ACTUAL resolved background with the caption over it, VO line, planned `visual_source:visual_query`, score, credit + per-scene WARNINGS (image didn't resolve → plain graphic, `ai` not wired, `stock_video` ≥~6s freeze risk, missing matchup flag/sticker). Also writes `<stem>.summary.json` for the command to report. `prepare.mjs` gained a no-VO storyboard branch (synthesizes scenes from the spec, durations from `duration_seconds`, writes `storyboard-props.json` — never clobbers the render's `props.json`). |
| `pipeline/upload_youtube.py` | **YouTube posting (DEFAULT)** — uploads a rendered MP4 straight to the channel via the official YouTube Data API (`videos.insert`); no Postiz/Docker. Title/description/tags from the spec; vertical 1080×1920 auto-classifies as a Short. Owner OAuth (`youtube.upload` scope) in `pipeline/.secrets/yt_upload_token.json` (reuses the analytics Desktop client). `auth` / `auth-finish` / `upload <spec> <mp4> [--visibility]`. Moves the render to `out/published/` on success |
| `pipeline/post_packet.py` | **TikTok/IG handoff** — `post_packet.py <spec> <mp4>` prints a per-platform JSON packet (absolute mp4 path + tiktok/instagram captions, formatted exactly like `publish._caption_for`) consumed by the **`post-social`** skill |
| `pipeline/publish.py` | **Legacy/fallback posting via Postiz** — uploads a rendered MP4 + spec caption/title/hashtags to Postiz (`channels` / `publish` subcommands), which moves the render to `out/published/`. Superseded by `upload_youtube.py` (YouTube) + the `post-social` skill (TikTok/IG); kept as a fallback only |
| `pipeline/regen_scene.py` | **Single-clip VO re-record** — `regen_scene.py <spec_stem> <scene_index>` regenerates ONE scene's mp3 (applies `pronunciations.json`) + patches `out/assets/<stem>/props.json`, so a one-line VO edit doesn't re-spend ElevenLabs credits on the whole video (then re-run `prepare.mjs` + render) |
| `pipeline/youtube_analytics.py` | **Real retention diagnostic + collector** — `summary` / `retention <video>` via the YouTube Analytics API (owner OAuth in `pipeline/.secrets/`), plus `collect [--stem --min-age-days]` which joins every matured published video's analytics with its spec's design levers + scene-leak mapping → `analytics/performance.jsonl` (feeds the `analytics-review` learning loop). ⚠️ Analytics API lags ~2–3 days; a fresh video returns zeros until processed (`collect` skips it as no-data) |
| `out/published/`, `logs/` | `out/published/` = published renders (moved by `upload_youtube.py`/`publish.py`); `out/renders/` = unpublished drafts; `logs/` = build/render logs. `out/published/post-log.jsonl` = TikTok/IG browser-post log (written by the `post-social` skill) |
| `.claude/skills/post-social/` | **TikTok + Instagram posting (DEFAULT)** — attended Playwright-MCP browser runbook: drives a logged-in browser to upload the MP4 + caption. Owner watches/clears any captcha/login. YouTube does NOT go through here (use `upload_youtube.py`) |
| `deploy/postiz/` | **Legacy/fallback** self-hosted **Postiz** scheduler (Docker compose + Temporal stack + setup README). No longer the default path — the hybrid poster (YT API + browser TikTok/IG) replaced it; `docker compose ... down` when not needed |
| `logo-icon.svg`, `logo-wordmark.svg` | Brand assets (gold/black; "TT" monogram + wordmark) |
| `.claude/skills/find-skills/` | Installed skill for discovering more skills |
| `.claude/commands/find-topics.md` | **`/find-topics` slash command** — ideation: what's TRUE now (`soccer-news`) × what's VIRAL (`outlier_ingest.py`) × what WORKS for us (`kb/learnings.json`) → a ranked shortlist of ~5 grounded, copyright-safe video ideas. Feeds `/daily`; does not script/render/post |
| `.claude/commands/daily.md` | **`/daily` slash command** — front of the pipeline: ground today's news (`soccer-news`) → pick an angle → `videospec` → `fact-check` → `make_video.sh` FREE draft → stop for owner review. Never spends ElevenLabs or posts (production + posting are owner-approved separately) |
| `.claude/commands/publish.md` | **`/publish` slash command** — last mile: confirm → YouTube via `upload_youtube.py` (official API, auto-logs + moves to `out/published/`) → TikTok/IG via `post_packet.py` + the attended `post-social` skill. Refuses `-draft` files; AskUserQuestion gate before posting (outward-facing) |
| `.claude/commands/analyze-channel.md` | **`/analyze-channel` slash command** — back of the pipeline: `inventory --backfill` → `collect` → `analytics-review` → report what's working/wrong + update `kb/learnings.json` |
| `.claude/commands/storyboard.md` | **`/storyboard` slash command** — pre-render preview: runs `storyboard.sh` (no VO/render) → reports the per-scene visual table + warnings + the HTML path. Slots into `/daily` between `fact-check` and `make_video.sh` so the draft is right the first time. Never spends credits, never renders, never posts |
| `pipeline/dashboard/` | **Mission Control dashboard** — a local, **token-free** control room (`bash pipeline/dashboard/run.sh` → http://localhost:8770; stdlib-only Python, binds 127.0.0.1). `server.py` aggregates `out/` state (specs → storyboard → draft → production → published, joined with `post-log.jsonl`) and fires the **deterministic** stages as background jobs with a live-streaming transmission log: **storyboard** (`storyboard.sh`), **draft** (`make_video.sh`), **production** (`TTV_PRODUCTION=1`, confirm-gated — spends credits), **publish YT** (`upload_youtube.py`, confirm-gated, refuses `-draft`), **outlier feed** (`outlier_ingest.py`). Serves storyboard contact-sheets + plays renders in-page (MP4 range). **No Claude is invoked from the server** (no token spend, no autonomous-agent surface) — brain steps (`/find-topics`, `videospec`, `/daily`) stay in the Claude session; briefs saved to `out/topics/*.md` + new `out/specs/*.json` auto-appear. Broadcast control-room UI (`index.html`, built with the frontend-design skill). |
| `HARNESS-PORTABILITY.md` | **Cross-harness guide** — how the skills + slash commands run outside Claude Code (OpenCode first). Skills are read natively by OpenCode from `.claude/skills/`; commands are translated to `.opencode/command/`; `CLAUDE.md` is bridged to `AGENTS.md`. |
| `pipeline/sync_harness.py` | **Harness sync generator** — keeps `.claude/` as the single source of truth and regenerates the harness-specific copies: translates `.claude/commands/*.md` → `.opencode/command/*.md` (drops `allowed-tools`/`argument-hint`, qualifies `model` to `provider/model`), bridges `AGENTS.md`→`CLAUDE.md`, sanity-checks skills. `sync` (write) / `check` (drift, exit 1 if stale). Pure stdlib. Re-run after editing any command or adding a skill. |
| `.opencode/command/`, `AGENTS.md` | **Generated, do not hand-edit** — OpenCode-format command copies + the instructions bridge, produced by `sync_harness.py`. Edit the `.claude/` source, then re-run sync. |

## Status & decisions

- ✅ **Brand chosen:** TikiTakaFootyTV — only shortlist name that was free as a *matching set* on IG + YouTube. Backup: TikiTakaFootyHQ.
- ⬜ **Handles NOT yet registered.** Owner should register `@tikitakafootytv` on IG + YouTube + TikTok (confirm TikTok at signup; fall back to `…hq`), same day, + grab the `.com`.
- ✅ Strategy, viral formula, pipeline design, outlier-engine spec, and seed list v1 are done.
- ✅ **Brain built:** the `videospec` skill (BRAND_SYSTEM + viral rules) + the `VideoSpec` schema (`videospec_schema.py`). Runs on the subscription.
- ✅ **Pipeline v1 renders real MP4s:** `pipeline/generate_assets.py` (ElevenLabs VO, via `uv`) → `pipeline/remotion/` (Remotion CSS graphics + assembly). First video at `out/renders/lamine-yamal-trajectory.mp4` (1080×1920, ~34s). Marginal cost ≈ ElevenLabs VO only.
- ✅ **Grounding engine built:** `kb/` (canonical KB) + the `soccer-news` skill (refresh + ground). Seeded with a **real, sourced** Lamine Yamal entry + the `yamal-next-messi` narrative. `videospec` now grounds before generating (no more stale "he's 16"). `kb/watchlist.json` lists entities still to populate via refresh.
- ⚠️ **Nano Banana image API needs billing** (free tier = 0). v1 uses **free Remotion CSS graphics** (crisper for stat text anyway); enable billing later for AI backgrounds (~$0.04/img).
- ✅ **Viral engine MVP built:** `pipeline/outlier_ingest.py` (deterministic; YouTube Data API + SQLite) — resolves seeds → ingests uploads/stats → ranks outliers by views/channel-median. First run found **Tifo's "Can you name X's top 5…" quiz format massively overperforming** (copyright-safe; worth adding to the format library). ~21 quota units/run.
- ✅ **Visual layer — per-scene progression + specificity routing:** each scene gets its own background matched to the VO. **Source by specificity:** `entity` (a SPECIFIC player/club/stadium/nation → free CC image from the KB; map clubs→stadium, e.g. `camp-nou`) → `ai` (specific, no CC; needs billing) → `stock` (Pexels, GENERIC atmosphere only) → `graphic`. `fetch_images.py` now fetches CC images for any entity type (players + stadiums + nations); the **KB is a visual library**, not just players. `videospec` PLANS the routing per scene (`visual_source`/`visual_query`); Remotion Ken-Burns'es each with per-scene credits. On-screen text **auto-fits** (`@remotion/layout-utils` `fitText`) so it never overflows the frame; keep `stat_callout` short (a number/≤2 words), longer lines go in `on_screen_text`. A **subject sticker** anchors the top — an iPhone-style **cutout** (`pipeline/cutout.py`, rembg → `out/cutouts/`) of the spec's `subject`, white-outlined and pinned top-right (circular-avatar fallback if no cutout exists yet). Default TTS voice: **Brian** (premade `nPczCjzI2devNBz1zQrb`, free-accessible — the paid-library `Gubgw9l4dtIoQA9YZHgx` is the same voice but blocked on free tier). Latest: `out/renders/lamine-yamal-v4-specific.mp4` (Camp Nou on the Barcelona beat). ⚠️ Remotion component is `TikiTakaVideo.tsx` (with the 'i'); composition id `TikiTaka`.
- ✅ **Hands-off pipeline + one-command driver:** `pipeline/ensure_assets.py` auto-creates stub entities, fetches their CC images, and auto-cuts-out the subject for any spec. `bash pipeline/make_video.sh out/specs/<stem>.json` runs the whole deterministic chain (ensure → VO → visuals → render) in one command. Proven end-to-end on a fresh subject: `out/renders/lionel-messi-last-dance.mp4` (Messi's image + cutout auto-fetched from scratch). The remaining brain step runs on the subscription: `soccer-news` (ground) → `videospec` (spec) → **`fact-check`** (verify claims vs KB) → `make_video.sh`.
- ✅ **Quality passes:** rendered text is **emoji-stripped** (emoji belong in captions only; the end-card 👇 is a drawn arrow); the **comment-bait is voiced** on the end card (outro VO); a **`commons`** visual source (Wikimedia Commons search) sources specific real things (the World Cup trophy, landmarks) that generic stock lacks. Visual routing: `entity` (KB) → `commons` (specific search) → `stock` (generic) → `ai` (needs billing) → `graphic`.
- ✅ **Image correctness (keyword search returns plausible-but-WRONG images):** (a) recurring subjects are **curated** as vision-vetted KB entities — a growing content library: trophies (`fifa-world-cup-trophy`, `champions-league-trophy`, `ballon-dor`), stadiums (`camp-nou`, `santiago-bernabeu`, `wembley-stadium`), plus players — correct by construction; (b) one-off `commons` scenes are **vision-vetted** by the `pick-images` skill (Claude looks at candidates → `choices.json`, which `prepare.mjs` consumes); (c) **all downloads are validated as real images** (reject HTML error pages). Curate a new recurring subject via `fetch_candidates.py "<query>"` → look → write the pick into a KB entity. Pipeline order: `videospec → fact-check → fetch_scene_candidates → pick-images → make_video.sh`.
- ⬜ **AI imagery (Nano Banana):** the `ai` visual_source isn't wired in `prepare.mjs` yet — needs Google **billing** enabled, then add the Nano Banana branch. Until then `ai` scenes render with no background.
- ⬜ **Not built yet:** resolve the 15 name-only seeds (done — see seeds.json); the outlier→formula **dissection skill**; one-command `spec → mp4` wrapper; voice/font finetuning; schedule the ingest + KB refresh; re-fetch Messi/Vinícius portraits (transient API fail).
- ✅ **Posting last-mile (Postiz) — PROVEN end-to-end:** first video published to the live YouTube channel **TikiTakaFootyTV** via the full automated chain (`videospec → fact-check → make_video.sh → publish.py → Postiz → YouTube`). Self-hosted scheduler at `deploy/postiz/` (`docker compose up -d`, UI http://localhost:4007); stack = Postiz + Postgres + Redis + Temporal. `pipeline/publish.py`: `channels` lists integrations, `publish <spec> <mp4> --integration <id> --platform youtube [--visibility public|unlisted] [--when now|<ISO>]` uploads the MP4 + creates the post (YT title/description/tags from the spec; vertical 1080×1920 auto-classifies as a Short). **Gotchas (all fixed + documented in `deploy/postiz/README.md`):** (a) public API base is `/api/public/v1` (NOT `/public/v1` — that 307s to /auth); (b) YT settings schema: `type`=privacy (`public|private|unlisted`), `selfDeclaredMadeForKids`=`"no"` string, tags=objects; (c) worker fetches media at `127.0.0.1:4007` internally but the app listens on :5000 → `ECONNREFUSED`; fixed by the **`postiz-loopback`** socat sidecar (forwards internal 4007→5000) baked into the compose. **Owner one-time setup:** Google Cloud OAuth app (YouTube Data API v3) → `YOUTUBE_CLIENT_ID/SECRET` in compose → connect channel → Public API key into `.env` as `POSTIZ_API_KEY`. ⚠️ OAuth app must be **published to Production** or the YT connection dies every 7 days (Testing-mode refresh-token expiry). We never upload footage — only our rendered MP4s.
- ✅ **WC launch + renderer upgrades (2026-06-11):** the World Cup opener video (`out/specs/wc2026-opener-history-repeats.json`, "history repeats" — Mexico vs South Africa mirrors the 2010 opener) was built, fact-checked, and **scheduled to publish 9am ET June 11** as a public YouTube Short. New renderer features (all in `pipeline/remotion/src/`): **VsSplit** now shows a flag/photo per side; **ScorersSplit** (`graphic_type: scorers_split`) = two scorers slide in (CC photo, or silhouette fallback) with the score between; the corner **sticker floats/hovers**; the **end card + comparison scenes take atmosphere backgrounds**; and a **Shorts SAFE-AREA** layout keeps all text/graphics clear of the platform UI (bottom caption bar + lower-right action rail) — reserved via `SAFE_TOP/SAFE_BOTTOM/PAD_L/PAD_R` in `TikiTakaVideo.tsx` (fixed a `padding` shorthand bug that pinned text too low).
- ⚠️ **Publish reliability:** scheduled posts FAIL when the YouTube OAuth token expires (the Testing-mode 7-day refresh-token expiry — see Postiz bullet). Diagnose before trusting a schedule: check `Integration.refreshNeeded/disabled` + `Post.state` in the Postiz DB, and test the refresh token against `https://oauth2.googleapis.com/token` (tokens are stored plaintext in the DB). Permanent fix = publish the Google OAuth app to **Production**.
- ✅ **Hybrid poster replaces Postiz as the default (2026-06-12):** to stop running the heavy always-on Postiz Docker stack, posting now splits by platform. **YouTube** → `pipeline/upload_youtube.py`, a lightweight standalone uploader hitting the official Data API directly (`videos.insert`); owner OAuth with the `youtube.upload` scope in `pipeline/.secrets/yt_upload_token.json` (reuses the analytics Desktop OAuth client + already-Production consent screen → no 7-day expiry). One-time: add the `youtube.upload` scope on the consent screen, then `auth`/`auth-finish` once. **TikTok + Instagram** → the attended **`post-social`** skill: Claude drives a logged-in browser via the **Playwright MCP** (`post_packet.py` builds the per-platform caption+path handoff). ⚠️ **Attended only** — TikTok/IG detect automated browsers, so the owner watches and clears any captcha/login/2FA; never run unattended/scheduled (residual shadowban risk accepted; YouTube stays on the safe official API precisely for this reason). Postiz (`pipeline/publish.py` + `deploy/postiz/`) is kept as a fallback but no longer runs by default (`docker compose -f deploy/postiz/docker-compose.yaml down`). New default chain: `videospec → fact-check → make_video.sh → upload_youtube.py (YT) + post-social skill (TikTok/IG)`.
- ✅ **Content pivot → daily news & stories + `videospec` retuned (2026-06-12):** channel identity moved from quizzes/stats to **daily World Cup news & stories** (narrative shorts; Pulisic redemption video = the model). Added `news_story` + `player_story` to the `Format` enum (`videospec_schema.py`); existing specs (`shock_stat` etc.) still validate. Retuned the `videospec` skill: story-first intro, the old "RESULT/MATCH-RECAP FRAMING" generalized into **"STORY, NEWS & RECAP FRAMING"** (covers player stories + news beats), and the FORMAT LIBRARY now leads with the two story formats (quizzes/stats demoted to "seasoning"). First TikTok post (Pulisic) shipped via the `post-social` browser flow. ⬜ Still TODO: re-fact-check old quiz specs' relevance; consider running skill-creator's eval + description-optimization loops on `videospec`.
- ✅ **Running TV scoreboard for match summaries (2026-06-12):** the `matchup` badge now becomes a persistent broadcast **scoreboard** (top-left, under the watermark: `flag CODE  H – A  CODE flag`) when scenes carry a per-scene **`score`** ("HOME-AWAY", home = `matchup[0]`). It starts 0-0 and **ticks up as the story is told**, the gold score **popping/flashing** when a goal goes in, and holds the final score through the end card. The running score now lives ONLY there — the big center `stat_callout` score is dropped so the middle is free for visuals. New `Scene.score` field (`videospec_schema.py`); plumbed via `prepare.mjs` (also resolves a 3-letter team `code` per side — add `code` to nation entities, e.g. usa→`USA`, paraguay→`PAR`; falls back to a short alias/first-3-letters); `Scoreboard` component in `pipeline/remotion/src/TikiTakaVideo.tsx` (falls back to the old flag-**VS** `MatchupBadge` when no scene has a `score`). `videospec` skill documents the pattern. First use: `out/specs/usa-paraguay-balogun-brace.json` (USA 4-1 Paraguay opener; subject Folarin Balogun — owner-provided photo in `assets/source/`, cutout at `out/cutouts/folarin-balogun.png`).
- ✅ **Group-standings TABLE + scoreboard control + owner-photo backgrounds (2026-06-14):** for WC group-stage recap videos, a scene can carry a **`group_table`** (list of `{team, points, played?, gd?, highlight?}`, ordered by rank) with **`graphic_type: "group_table"`** → renders a clean ranked table (position + flag + 3-letter code + P/GD/PTS, the video's team highlighted gold). New `GroupTable` component (`TikiTakaVideo.tsx`), `GroupRow` model + `Scene.group_table` (`videospec_schema.py`), flags/codes resolved in `prepare.mjs` via the shared `nationFlag()` helper. **The running scoreboard auto-HIDES on any group-table scene AND on the comment-bait end card** (the Scoreboard now spans content-only + takes a `hidden` ranges list; EndCard `topPad` dropped when a scoreboard video). **Scoreboard position (2026-06-14): CENTERED in the lower-middle** (`SCOREBOARD_TOP`/`COMP_H` consts) — moved off the top-left where it covered player faces; captions reserve space above it (`padBottom` in `Card` when `hasScoreboard` + not a group-table beat) so they never overlap. Applies to all scoreboard videos. ⚠️ **The scoreboard needs FLAG images on BOTH `matchup` nations** — if a nation entity has `image:null`, matchup resolution throws and the scoreboard silently doesn't render (this bit the AUS-TUR video until `australia`/`turkiye` got flag images). **Owner-provided LOCAL images now work as entity BACKGROUNDS, not just cutouts:** set a KB entity's `image.url` to a repo-relative path like `assets/source/<name>.jpg` (blank `attribution` so no credit burns in) — `prepare.mjs download()` + `cutout.py` both copy local files now. ⚠️ **Crowded match shots → crop to the lone player BEFORE `cutout.py`** (rembg grabs every foreground body; the Irankunda shot needed the opponent cropped out first). ⚠️ **Image-correctness still bites:** an owner-supplied "Irankunda" photo was actually a different player (a Burundi-kit player — "Irankunda" is a common Burundian surname); always eyeball owner photos for the RIGHT person. First use: `out/specs/australia-turkiye-irankunda-shock.json` (Group D table; subject Nestory Irankunda; foil background Arda Güler, both owner photos). **Group-table scene conventions (2026-06-14, owner — ALWAYS):** (a) the table scene uses the **`night-stadium` `stock_video`** background (`visual_source: "stock_video"`, `visual_query: "night-stadium"`, NOT `graphic`); (b) the **VO karaoke captions now render UNDER the table** — the `Card` `group_table` branch stacks `GroupTable` + `Karaoke` in a centered column with a gap so they never overlap; use `on_screen_text` as the gold heading (e.g. `"GROUP E"`) and keep the table-scene VO under ~6s (night-stadium clip freeze threshold).
- ✅ **Corner stickers DISABLED entirely (2026-06-14, owner directive):** no subject cutout, no per-scene flag chip. The top-level subject cutout (`Props.sticker`) was already not rendered; the per-scene `FlagSticker` render was removed from `TikiTakaVideo.tsx` (component + `sticker`/`sticker_entity` fields left defined but intentionally never rendered, for back-compat). The matchup **scoreboard/VS badge** already carries both flags, so a corner element is redundant + crowds the frame — and it removes the need to onboard a subject photo just for a sticker. `videospec` updated: `subject` is now metadata/thumbnail identity only, `sticker_entity` is deprecated (leave `""`). Don't reintroduce a corner sticker.
- ⚠️ **`stock_video` b-roll FREEZES if a scene outlasts its clip:** curated clips are ~6s and `OffthreadVideo` has **no `loop`** in our Remotion (4.0.474) — it holds the last frame once the clip ends. Keep a `stock_video` scene's VO short enough to fit under ~6s (the VO drives scene length). ⚠️ A frozen bg is NOT reliably detectable by frame-diffing the MP4 — the karaoke captions (and scoreboard) change every frame, so md5s differ even when the background is frozen; eyeball it. Real fix for long beats later = generate longer clips or wire a true loop (Remotion `<Video loop>`). New **action-implied** b-roll prompts added to `assets/broll/AI-BROLL-PROMPTS.md` (`net-ripple`, `ball-to-net`, `knee-slide`, `wheel-away`) so goal beats can DEPICT the goal (faceless) instead of leaning on a static portrait or generic crowd — pending generation.
- ✅ **Learning loop closed (2026-06-13):** analytics now feed back into creation. `upload_youtube.py` logs each upload's `stem→video_id` to `out/published/post-log.jsonl`; `youtube_analytics.py collect` joins each matured video's analytics (retention curve, swipe-away, conversion) with its spec's design levers + a **scene-leak mapping** (which scene the biggest retention drop lands in) → `analytics/performance.jsonl`. The new **`analytics-review`** skill (`review` mode) interprets that store and distills dated, evidenced findings into `kb/learnings.json`; **`videospec` reads it before scripting and auto-applies `high`-confidence learnings** (step 1b), weighting `low`/`med` as tie-breakers — superseding the old hand-typed "video #1" calibration block. **Tiny-N discipline is enforced in the skill:** generalizations are hypotheses (`confidence:"low"` until N≥5), per-video facts are solid, no viral rule is deleted on 1–2 videos. **On-demand** (analytics lag ~2–3 days makes daily pointless); run **`/analyze-channel`** (slash command) or the `analytics-review` skill a few days after a batch of posts.
- ✅ **First full channel analysis + pipeline upgrades (2026-06-13):** ran the loop across all 8 published videos (~6.4k views, 19 subs). Upgrades: `youtube_analytics.py` gained an **`inventory [--backfill]`** subcommand (discovers the whole channel via the Data API by @handle → uploads playlist, flags/auto-logs videos missing from the post-log by title-matching specs — recovered the 7 pre-logging uploads), and **`collect` now merges near-real-time public stats** (Data API views/likes/comments, no lag) with Analytics retention (matured only) so `analytics/performance.jsonl` reflects the whole channel immediately. **Fixed a metric bug:** `audienceWatchRatio` can exceed 100% (loops), so the old `swipe_away = 1 − start` was nonsense (gave −64%); the record now uses `averageViewPercentage` (clean headline) + relative `retained_to_end`/`early_leak`/`biggest_leak`/`mean_relative_performance`. **Findings (3 matured):** `quiz_top5` is the clear laggard (~370 avg views + 13–19% retained-to-end vs ~1000+ views + 40% for the Messi `shock_stat`) → validates the news/story pivot; **subscribe conversion ~0.18% is the bottleneck, not reach**; keep ≤~35s; quizzes still pull the most comments. Wrote these to `kb/learnings.json` (low/med confidence — N=3). ⚠️ **Metric nuance:** the "68% swiped away" is a YT *Studio impression-level* stat, DIFFERENT from the Analytics `audienceWatchRatio` curve (measured among counted views past the start) — both leaks are real; don't read the curve's small early-drop as "hook solved."
- ✅ **Pre-render storyboard (2026-06-14):** new **`/storyboard`** command (`pipeline/storyboard.sh` + `pipeline/remotion/storyboard.mjs`) previews a spec as an HTML contact sheet **before** the slow Remotion render — one 9:16 card per scene showing the ACTUAL resolved background/sticker with the caption over it, the VO line, the planned `visual_source:visual_query`, the running score, and per-scene WARNINGS (visual didn't resolve → plain graphic, `ai` not wired, `stock_video` ≥~6s freeze risk, missing matchup flag/subject sticker). Faithful by construction: `prepare.mjs` gained a no-VO `TTV_STORYBOARD` branch (synthesizes scenes from the spec, durations from `duration_seconds`, writes `storyboard-props.json` — render's `props.json` untouched), so the storyboard uses the **exact same resolver** the render does. Spends no ElevenLabs credits and runs no render; the images it downloads are cached so the eventual draft is faster. Workflow: `videospec → fact-check → /storyboard (fix visuals in the spec, re-run free) → make_video.sh draft`. Proven on the AUS-TUR + Pulisic specs.
- 📌 Reference benchmark: **Subscribr.ai** — productized version of our "brain" half (ideation/script/thumbnail). Validates the approach; its moat is the outlier-data engine, which `OUTLIER-ENGINE-SPEC.md` replicates.

## Build order (when we start coding)

1. ✅ `videospec` skill + `VideoSpec` schema exist — now **tune the skill** against real generated specs.
2. One spec → ElevenLabs VO → one Remotion template → one end-to-end video.
3. Add auto-captions; add Nano Banana graphics stage.
4. Batch generation (whole matchday in one Batches call).
5. Outlier-engine MVP (seeds.json → daily ingest → median baselines → outlier feed).
6. ✅ Posting — first public YouTube post shipped via Postiz; **now replaced by the hybrid poster** (`upload_youtube.py` for YT + the attended `post-social` skill for TikTok/IG). Postiz retained as fallback.

## Working conventions

- **LLM/scripting runs on the Claude Code subscription** (the `videospec` skill, or headless `claude -p`) — NOT the metered Anthropic API. Keys are only for Nano Banana, ElevenLabs, YouTube Data API. The `VideoSpec` Pydantic schema is the validation contract, not an API caller.
- **Tooling:** Python deps via **`uv`** (system `python3` has no pip). **Node + Remotion** for the video render stage. Secrets live in `.env` (see `.env.example`), never committed.
- **ElevenLabs credits = money — ALWAYS draft first.** `make_video.sh` defaults to a FREE Piper draft; only run `TTV_PRODUCTION=1` (real ElevenLabs VO) on a spec whose visuals/captions/pacing have been reviewed and approved. Caption/layout/image fixes are free to iterate in draft. If only the caption text (not audio) is wrong on an already-produced video, patch the words in `out/assets/<stem>/props.json` and re-run `prepare.mjs` + Remotion render — never re-run TTS just to fix on-screen text.
- **Grounding:** time-sensitive facts (age, club, fitness, form, records) MUST come from the `kb/` via the `soccer-news` skill — never from model memory (it's stale). Every KB fact is dated + sourced; verify freshness before grounding.
- **Superlatives need EXTERNAL, adversarial verification — never trust the KB to prove them.** Any "first/only/nobody/record/most/youngest" claim must be checked against current sources by actively searching for a counterexample (the `fact-check` skill), because the KB is always incomplete. Real example: the KB said only Messi & Ronaldo reach a 6th World Cup — it missed **Guillermo Ochoa** (three total). If the full set can't be established, reframe ("one of only a handful ever"), don't assert a false "first/only".
- **Claude/Anthropic API questions:** consult the `claude-api` skill — don't answer model IDs/pricing/caching from memory.
- Keep docs current when decisions change (this file + PLAYBOOK especially).
- Default model for the content engine is **Opus 4.8** (`claude-opus-4-8`); drop to Sonnet/Haiku only for mechanical stages.
- Verify any memory/recalled fact (handles, file names) before acting — handle availability was checked, not registered.
